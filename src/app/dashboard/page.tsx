/**
 * Dashboard home — Phase 1 placeholder.
 *
 * The real workspace (header stats, collection cards, recently-used items)
 * is built in later phases. For now this is just a labelled main area.
 */
export default function DashboardPage() {
  return (
    <h2 className="text-lg font-semibold text-muted-foreground">Main</h2>
  );
}
